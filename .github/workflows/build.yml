name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: "10.x"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore DevHabit.Api/DevHabit.Api.slnx

      - name: Build
        run: dotnet build DevHabit.Api/DevHabit.Api.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test DevHabit.Api/DevHabit.Api.slnx --configuration Release --no-restore --no-build

      - name: Publish
        run: dotnet publish DevHabit.Api/DevHabit.Api.slnx --configuration Release --no-restore --no-build